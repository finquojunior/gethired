# History

Point-in-time reviews kept for context only. Each was written against the
codebase as it stood on its date (August 2026) and every finding they raised
has since shipped or been deliberately closed; nothing here describes current
behaviour. For what is true today read the top-level docs: the deploy runbook,
the security audit, and the latest usability audit in `docs/`.
